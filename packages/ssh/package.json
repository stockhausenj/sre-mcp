{
  "name": "@sre-mcp/ssh",
  "version": "1.0.0",
  "description": "SSH MCP server for remote command execution",
  "type": "module",
  "bin": {
    "ssh-mcp-server": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "ssh2": "^1.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ssh2": "^1.15.0",
    "typescript": "^5.3.0"
  }
}
